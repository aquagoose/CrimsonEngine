#pragma once

#define VertexTexture layout (set = 0) ParameterBlock
#define VertexUniform layout (set = 1) ParameterBlock

#define PixelTexture layout (set = 2) ParameterBlock
#define PixelUniform layout (set = 3) ParameterBlock
